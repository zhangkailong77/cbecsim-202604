# 当前开发进度（实时）

最后更新：2026-04-16

### 1.9 根目录 README 本地开发说明补齐（2026-04-16）
- 已在仓库根目录新增 `README.md`：
  - 补充本地开发环境要求（Node.js、Conda、Python 3.12、MySQL、Redis）；
  - 补充前后端 `.env` 配置说明与关键变量示例；
  - 补充一键联启 `./start-dev.sh` 与前后端手动分开启动步骤；
  - 补充健康检查、自检流程、默认管理员账号与常见问题排查。
- 当前状态：新同学可直接按 README 完成首次本地调试启动。
- 下一步待办：如后续新增 Docker 化本地环境或初始化脚本，需同步更新 README。

## 1. 总体阶段
- 当前系统主链路已覆盖：`Step 01 开局 -> Step 02 选品采购 -> Step 03 物流清关 -> Step 04 入仓 -> Step 05 店铺运营（Shopee）`。
- 当前开发重心已从“页面复刻与表单打通”进入“运营模拟（买家池/订单生成）MVP”阶段。

### 1.1 历史对局库存漂移修复（2026-04-10）
- 已明确“库存漂移”定义：同一对局内出现 `库存件数 + 已售件数 != 入库总件数`（口径均为件）。
- 已定位主要成因：历史数据中存在“同一订单重复取消释放（cancel_release）”导致库存回补重复记账。
- 已完成防回归修复：`shopee_order_cancellation.cancel_order` 增加行锁与幂等保护，避免重复取消重复释放。
- 已新增对账脚本：
  - `backend/apps/api-gateway/scripts/reconcile_cancel_release_drift.py`（修复重复取消释放超额）
  - `backend/apps/api-gateway/scripts/reconcile_inventory_conservation.py`（按“入库-已售”守恒口径对齐库存）
- 已对 `run_id=3` 执行实库修复并验证：
  - 修复后：`库存件数 9916 + 已出售库存件数 84 = 10000`
  - 商品可售库存与库存批次可用库存已对齐。
- 历史经营记录（`/setup/history`）展示优化：
  - 已隐藏右侧“统一倒计时中心/阶段总览数据”实时卡片；
  - 历史回溯页改为主内容全宽展示，避免将历史对局误解为仍在实时进行。

### 1.2 Shopee 质量评分弹窗与 AI 总结优化（2026-04-10）
- 质量详情接口新增 `summary` 字段：
  - 后端优先读取模型返回的 `summary/evaluation_summary/overall_summary`；
  - 前端“评价总结”改为直接展示 AI 返回内容，不再使用分数阈值固定文案。
- 模型提示词已补充 `summary` 输出约束：
  - 要求输出 2~4 句整体总结，避免固定模板化措辞。
- 质量详情弹窗样式已调整为 Shopee 风格：
  - 白底矩形弹窗 + 深色遮罩 + 橙色主色强调；
  - 结构保持“总分卡片 + 维度分析 + 逐图评价”并强化可读性。

### 1.3 Shopee 营销中心复刻设计完成（2026-04-14）
- 已新增设计文档：`docs/设计文档/18-Shopee营销中心复刻设计.md`。
- 设计范围覆盖：
  - 前端高保真复刻布局（Announcement / Marketing Tools / Shopee Events）；
  - 后端接口规划（bootstrap + 公告/工具/活动/偏好）；
  - 数据库建模（公告、工具、活动、用户偏好四张表）；
  - Redis 缓存键设计、TTL、失效与限流策略。
- 当前状态：完成方案设计，尚未进入代码实现阶段。
- 下一步待办：按设计文档拆解前后端实施任务并进入开发联调。

### 1.5 Shopee 折扣页复刻设计完成（2026-04-14）
- 已新增设计文档：`docs/设计文档/19-Shopee折扣页复刻设计.md`。
- 设计范围覆盖：
  - 折扣首页高保真复刻（创建入口、类型 Tab、促销表现、筛选区、活动列表）；
  - 后端接口规划（bootstrap + 列表 + 表现概览 + 详情 + 复制 + 偏好）；
  - 数据库建模（活动主表、活动商品表、表现快照表、用户偏好表）；
  - Redis 缓存键设计、TTL、失效与限流策略；
  - 与订单归因、销量统计、历史回溯只读模式的联动口径。
- 当前状态：完成折扣页方案设计，尚未进入代码实现阶段。
- 下一步待办：按文档进入折扣页前端复刻与后端接口实现。

### 1.7 Shopee 单品折扣创建页设计完成（2026-04-14）
- 已新增设计文档：`docs/设计文档/20-Shopee单品折扣创建页设计.md`。
- 设计范围覆盖：
  - 创建页高保真复刻（基础信息、活动时间、添加商品、页脚操作）；
  - 商品选择器弹窗/抽屉设计；
  - 后端接口规划（bootstrap、可选商品、草稿、正式提交、详情）；
  - 数据库建模（活动商品扩展、草稿主表、草稿商品表、校验日志）；
  - Redis 缓存键设计、TTL、失效与限流策略。
- 当前状态：完成单品折扣创建页方案设计，尚未进入代码实现阶段。
- 下一步待办：按文档进入创建页前端复刻与后端接口实现。

### 1.8 Shopee 套餐优惠创建页宽度对齐（2026-04-15）
- 已完成 `BundleCreateView` 外层布局收口：
  - 内容区背景、左右留白、顶部留白与单品折扣创建页保持一致；
  - 主容器最大宽度已对齐单品折扣创建页；
  - 底部操作按钮区域间距已同步到同一节奏。
- 已按官方 `Create New Bundle Deal` 参考图继续重排：
  - 删除右侧手机预览，恢复“左侧主表单内容列”布局；
  - 基础信息区改为固定标签列 + 固定输入列（`200px + 600px`），避免内容铺满整卡；
  - 阶梯表头、行高、输入框宽度、`Add Tier` 位置与商品区间距按官方节奏收口；
  - 页面主展示文案（基础信息区与商品区）已切换为官方英文口径，便于高保真对照。
- 本次调整仅涉及前端布局，不改动套餐优惠表单字段、校验逻辑与提交流程。
- 当前状态：套餐优惠创建页已从“铺满式内容”收敛到官方样式的窄内容列布局。
- 下一步待办：基于实机截图继续微调标签列右边距与时间输入框宽度，做像素级贴近。

### 1.6 Shopee 折扣页首版实现完成（2026-04-14）
- 已完成 Shopee 折扣页前端接线：
  - 新增 `marketing-discount` 视图与路由解析；
  - 左侧菜单“折扣”可进入页面；
  - 营销中心首页点击“折扣”工具可进入真实折扣页；
  - 已完成创建卡片区、活动类型 Tab、促销表现区、筛选区、活动列表区首版复刻；
  - 历史回溯模式下允许浏览，并禁用创建/编辑/复制等写操作。
- 已完成后端折扣页首版接口：
  - `GET /shopee/runs/{run_id}/marketing/discount/bootstrap`
  - `GET /shopee/runs/{run_id}/marketing/discount/campaigns`
  - `POST /shopee/runs/{run_id}/marketing/discount/preferences`
- 已完成数据库接入：
  - `shopee_discount_campaigns`
  - `shopee_discount_campaign_items`
  - `shopee_discount_performance_daily`
  - `shopee_user_discount_preferences`
  - `shopee_orders` 新增营销活动归因字段
- 已完成 Redis 接入：
  - 折扣页 bootstrap 缓存；
  - 折扣活动列表缓存；
  - 折扣页缓存统一失效；
  - bootstrap / 列表访问限流。
- 当前状态：折扣页已具备真实数据驱动能力；创建/编辑/详情等深层功能页仍为下一阶段预留。
- 下一步待办：继续实现折扣活动创建页、编辑页与活动归因统计回写。

### 1.4 Shopee 营销中心首页首版实现完成（2026-04-14）
- 已完成 Shopee 前端营销中心首页接线：
  - 新增 `marketing-centre` 视图与路由解析；
  - 左侧菜单“营销中心”可进入首页；
  - 已完成 Announcement / Marketing Tools / Shopee Events 三大区块首版复刻；
  - 历史回溯模式下允许浏览并禁用偏好写入。
- 已完成后端营销中心首版接口：
  - `GET /shopee/runs/{run_id}/marketing-centre/bootstrap`
  - `POST /shopee/runs/{run_id}/marketing/preferences`
- 已完成数据库接入：
  - `shopee_marketing_announcements`
  - `shopee_marketing_tools`
  - `shopee_marketing_events`
  - `shopee_user_marketing_preferences`
- 已完成 Redis 接入：
  - 营销中心 bootstrap 缓存；
  - 用户级缓存失效；
  - bootstrap 访问限流。
- 已完成营销中心首页首版中文化展示：
  - 页面主标题、区块标题、按钮文案改为中文；
  - 营销工具名称、说明、标签文案改为中文映射显示；
  - 活动区入口文案、活动状态、公告徽标改为中文；
  - 左侧营销中心子菜单显示文案同步中文化。
- 当前状态：营销中心首页已具备真实数据驱动能力；各营销工具功能页仍为后续阶段预留。
- 下一步待办：继续实现折扣/代金券/活动等营销子模块页面与业务逻辑。

## 2. 已完成模块（开发侧）
### 2.1 工作台（Setup）
- 左侧菜单 + 右侧内容区架构稳定。
- 支持按菜单切换子视图，并与 URL 子路由同步。
- 统一时间中心与游戏日历组件已接入，支持实时游戏时刻显示与手动查看月份。

### 2.2 Shopee 店铺运营（Step 05）
- 新增商品主流程（第二步详情页）大部分结构已复刻并中文化。
- 核心板块：基础信息、规格、商品描述、销售信息、物流、其他。
- 图片/视频上传、类目选择、规格字段动态渲染、变体/批发价/物流配置等交互已打通到主要数据结构。
- “我的产品”列表已接入发布数据，支持父商品与变体展开查看、批量勾选、底部批量操作条。

### 2.3 管理员买家池总览（yzcube）
- 新增管理员入口菜单：`买家池总览`（仅 `super_admin + yzcube` 可见）。
- 新增买家画像首版（10人）并落库。
- 总览页支持：
  - 对局筛选（run_id）
  - 实时指标（买家总数、当前时段活跃估算、预估每小时订单）
  - Top3 活跃买家
  - 买家画像表格
- 右侧统一时间中心已支持按筛选对局同步显示（对局/玩家/游戏日/游戏时刻）。
- 新增“模拟一小时订单”手动触发按钮（管理员侧）。
- 触发后展示本次模拟摘要：激活买家数、候选商品数、生成订单数、跳过原因。

### 2.4 上架后自动模拟订单（P1.5）
- 已新增订单模拟服务层：复用买家池画像与概率参数，支持按对局产单。
- 已新增管理员触发接口：`POST /game/admin/runs/{run_id}/orders/simulate`。
- 已新增玩家触发接口：`POST /shopee/runs/{run_id}/orders/simulate`（用于调试）。
- 已新增订单模拟日志表：`shopee_order_generation_logs`（含表注释/字段注释维护）。
- Shopee 订单列表接口已扩展：返回最近1小时模拟新增订单数与最近模拟时间。
- 玩家端“我的订单”页已接入中文提示：
  - 顶部提示“最近1小时新增 X 单（买家池系统模拟）”
  - 订单行新增“系统模拟”标签
  - Tab 与关键字段文案中文化（待出货/运输中/已完成等）。
- 已修复手动模拟“激活买家长期为0”的关键问题：
  - 手动触发默认模拟时刻会按上次日志自动 `+1小时` 推进（不再重复同一小时）
  - 买家激活与下单采样改为每次触发独立随机抽样，避免固定哈希导致重复结果。
- 管理员“本次模拟摘要”已升级为可追溯明细：
  - 店铺上下文（run_id / user_id / 玩家 / 市场 / 状态）
  - 买家逐条决策流水（激活判定、候选商品、选中商品、SKU、价格、概率、最终原因）
  - 生成订单详情（订单号、SKU、数量、单价、实付）
- 买家池已扩容至 50 人（`BYR001~BYR050`），昵称统一英文名。
- 50 人均使用多峰活跃时段模型（早/午/晚多个活跃峰值），不再是单一活跃时段。
- 买家池激活曲线已升级为“作息型分层模型”：
  - 按早睡型/常规型/夜猫型/轮班型生成 24 小时活跃曲线；
  - 引入连续睡眠窗口（部分小时激活概率为 0）；
  - 睡眠前后 1 小时为软低活跃区间，减少离散随机噪声。
- 已修复“模拟订单不选择变体”问题：
  - 订单候选筛选改为支持“变体库存总和”参与判断（不仅看父商品库存）
  - 变体商品下单时按买家画像选择最匹配变体并扣减对应变体库存
  - 父商品库存与变体库存总和保持同步更新。
- 已修复“订单列表图片仍显示主图”问题：模拟订单明细图片优先写入变体图，缺失时才回退主图。
- 已修复 Shopee 订单页金额单位显示问题：订单金额前缀统一为 `RM`（马来站展示）。
- Shopee“我的订单”操作列已对齐真实结构：支持三项纵向操作（查看物流详情 / 查看详情 / 打印面单）。
- 已修复模拟订单物流渠道来源：下单时按商品上架勾选的物流方式（标准快递/标准大件/快捷快递）写入订单渠道，不再固定写死。

### 2.5 Shopee 履约发货与结算（09，手动推进版）
- 已完成订单履约数据结构扩展：
  - `sim_buyer_profiles` 增加 `city_code/lat/lng`。
  - `shopee_orders` 增加 `tracking_no/waybill_no/ship_by_at/shipped_at/delivered_at/eta_start_at/eta_end_at/distance_km`。
  - 新增 `shopee_order_logistics_events`、`shopee_order_settlements`。
- 已补齐新增表与新增字段注释映射（table/column comments）并接入初始化增量补字段逻辑。
- 已新增履约计算服务：距离（Haversine）、运费估算、ETA 计算、单号生成、结算计算。
- 已新增 Shopee 履约接口：
  - `POST /shopee/runs/{run_id}/orders/{order_id}/ship`
  - `GET /shopee/runs/{run_id}/orders/{order_id}/logistics`
  - `POST /shopee/runs/{run_id}/orders/{order_id}/logistics/progress`（手动推进）
  - `GET /shopee/runs/{run_id}/orders/{order_id}/settlement`
- 已完成前端“我的订单”接线：
  - 待出货支持“安排发货”。
  - 运输中展示 tracking_no 与 ETA 区间。
  - 物流详情支持查看轨迹并手动推进下一节点。
  - 已完成订单支持查看结算详情。
  - “查看物流详情”已升级为独立路由详情页：`/u/{public_id}/shopee/order/{order_id}?type={tab}`，可从详情返回原 tab 列表。
- 已实现物流“自动推进 + 手动加速并存”：
  - 自动推进按游戏时间 tick 执行（在订单查询链路自动补推进）。
  - 手动推进按钮保留，作为即时加速下一节点。
- 订单页补充运营效率交互：
  - 待出货页支持勾选、全选本页与批量发货。
  - 订单列表支持分页浏览（URL 保留 `page` 参数，含页码省略号样式）。
  - 后端已新增常驻自动订单tick worker：按游戏时钟持续补跑订单（默认每 15 秒循环、每次每个 run 最多补 2 个游戏小时），无需手动刷新页面。
  - 订单列表保留轻量兜底补偿（单次最多补 1 tick），避免首次加载大批量补算导致接口变慢。
  - 订单自动生成节奏已调整：从“每 1 游戏小时一个 tick”改为“每 8 游戏小时一个 tick”（约每 10 分钟真实时间一轮，worker 与订单页兜底补偿保持一致）。
  - 物流自动推进已调整为“按运输游戏天数里程碑推进”，不再按固定每小时推进节点：发货时会读取 Step03 最新线路（经济/标准/快速）并结合仓到买家距离计算预计运输天数，达到预计签收时间后才自动进入已签收。
  - Step03 国际物流与清关页新增调试加速能力：在保留自动推进前提下，支持一键将指定物流单加速到“清关完成”（按游戏日时长换算回拨运单创建时间实现）。
  - 已修复“线路文案与 ETA 来源不一致”问题：发货时将真实履约线路（`delivery_line_key/label`）固化到订单，后续运输中列表与物流详情统一读取订单级线路；历史订单无该字段时会按 ETA 反推线路作为兜底展示。
  - 订单列表（运输中）与物流详情页已补充时效解释展示：配送线路、线路时效区间、预计/已运输/剩余天数，便于玩家理解 ETA 来源。
  - 已修复订单列表偶发 500：结算写入在并发推进下可能触发 `shopee_order_settlements.order_id` 唯一键冲突，现改为并发安全幂等 upsert（savepoint + flush，冲突时回退为 update）。
- 商品列表显示优化：
  - 已新增变体销量字段 `shopee_listing_variants.sales_count`（模型、增量补字段、字段注释、接口返回）。
  - 下单模拟会同步累加对应变体销量，商品列表支持按变体显示“销量 + 库存”真实值。
- 商品编辑语义修正：
  - 编辑已上架商品时，“更新”按钮改为仅更新商品信息（保持原上架状态，不再按“重新发布”语义处理）。
  - 编辑页底部操作文案统一中文：`取消 / 下架 / 更新`。
  - 编辑模式页面文案区分已完成：从“编辑”进入时，顶部面包屑末级与页面标题显示为`产品详情`；从“添加新商品”进入时仍保持`添加新商品`。
  - 已修复编辑更新导致“变体图片/变体销量丢失”问题：编辑发布时按 `source_variant_id -> SKU -> 变体选项` 关联旧变体，默认保留历史 `image_url` 与 `sales_count`，仅在重新上传变体图时覆盖图片。
  - 已修复编辑更新导致“变体 ID 变化”问题：编辑发布改为“就地更新已存在变体”，不再先清空重建；仅对确实被删除的变体做删除、对新增变体做新增，因此仅改库存等字段时变体 ID 保持不变。

### 2.6 买家取消订单（10，超时未发货）
- 已完成后端取消字段打通：`cancelled_at/cancel_reason/cancel_source`（模型 + 历史库补字段 + 字段注释）。
- 已扩展取消轨迹事件：`cancelled_by_buyer`（买家取消订单）。
- 已实现自动取消（游戏时间驱动）：
  - 仅 `toship` 参与判定。
  - 超时阈值：满 48 游戏小时后才开始计算取消概率。
  - 取消概率：`min(0.90, 0.25 + (overdue_hours - 48) * 0.08)`。
- 已新增手动取消接口：`POST /shopee/runs/{run_id}/orders/{order_id}/cancel`（仅 `toship`）。
- 已完成订单列表/详情打通：
  - `type=return_refund_cancel` 可筛出 `cancelled` 订单。
  - “退货/退款/取消”Tab 计数改为真实后端统计。
  - 取消订单在列表/详情展示取消时间、取消原因、取消来源，并禁用物流推进。
- 已补齐取消回库逻辑：
  - 订单取消时自动回补库存（优先回补对应变体库存，并同步父商品库存）。
  - 同时回滚销量（父商品与命中变体 `sales_count` 扣减，不低于 0）。
- 已完成管理员侧可视化：
  - 管理员“模拟一小时订单”接口回传 `cancellation_logs`。
  - 买家池总览页“本次模拟摘要”新增“本次取消日志”区块，可查看买家、订单号、取消时间、超时小时、概率、来源与原因。
  - 买家池总览页已支持“自定义快进小时数（1~168）”并连续模拟，摘要按本次快进总时长聚合展示。

### 2.7 卖家回款与余额流水（11，MVP）
- 已新增财务流水模型：`shopee_finance_ledger_entries`（含唯一约束 `order_id + entry_type`，用于防重复回款）。
- 已补齐该表 table/column comments 映射，纳入数据库注释维护逻辑。
- 已实现回款幂等入账服务：
  - 仅 `completed` 且非取消订单可回款；
  - 仅存在 `settled` 结算记录才入账；
  - 并发场景通过唯一约束 + 捕获冲突保证不重复入账。
- 已接入回款触发点：
  - 物流推进至 `delivered/completed` 时自动写入回款流水；
  - 订单/财务查询时补偿回填历史 completed 订单未入账流水。
- 已新增财务接口（MVP）：
  - `GET /shopee/runs/{run_id}/finance/overview`
  - `GET /shopee/runs/{run_id}/finance/transactions`
  - `GET /shopee/runs/{run_id}/finance/income`
- 已新增 Shopee 财务前端页面（MVP）：
  - `我的余额`（余额总览 + 流水筛选 + 分页）
  - `我的收入`（收入概览 + 回款订单列表 + 分页）
  - 侧边栏菜单、页面路由、顶部面包屑已接入。
- 已完善“我的收入”日期筛选交互（对齐 Shopee 结构）：
  - 悬浮预览、点击选中（未点击不改最终筛选）
  - 中文预设项：本周 / 本月内 / 过去三个月 / 选择日期
  - 支持选择日期后展开双月日历范围
  - 预设时间范围按游戏时间锚点（`finance/overview.current_tick`）计算，不使用浏览器本地当前时间。
- 已修正“我的收入-已入账汇总口径”：
  - `本周` 使用 `week_income`（按游戏时间周起点汇总）
  - `本月` 使用 `month_income`（按游戏时间月起点汇总）
  - `累计` 使用 `total_income`（历史累计）
- 已补齐“我的收入”待入账与已入账分区差异化展示：
  - 待入账 Tab 使用独立表头（订单 / 预计入账日期 / 状态 / 支付方式 / 待入账金额）
  - 待入账数据源改为订单侧 `运输中 + 已完成但未到释放日` 聚合（明确排除待出货，避免将可取消订单计入待入账），列表结构与已入账分离。
- 已落地“订单完成后 3 游戏天释放回款”：
  - 回款入账不再在签收瞬间写入，改为 `delivered_at + 3天` 才允许入账；
  - 前端待入账列表会展示预计释放时间并区分状态（等待订单完成 / 等待打款释放）。

### 2.8 添加银行账户（12，已打通）
- 已新增银行账户数据模型：`shopee_bank_accounts`（含唯一约束 `run_id + user_id + account_no`）。
- 已补齐该表 table/column comments 映射，纳入数据库注释维护逻辑。
- 已新增银行账户后端接口：
  - `GET /shopee/runs/{run_id}/finance/bank-accounts`（列表）
  - `POST /shopee/runs/{run_id}/finance/bank-accounts`（新增）
  - `POST /shopee/runs/{run_id}/finance/bank-accounts/{account_id}/set-default`（设默认）
- 已完成前端银行账户页打通：
  - 点击“添加银行账户”卡片弹出新增弹窗（姓名/银行/账号/默认账户）
  - 点击“保存”直接校验并落库，成功后刷新卡片列表
  - 卡片支持“设为默认”
- 已完成余额页联动：`我的银行卡` 区域改为读取真实默认银行账户；无账户时显示引导文案。

### 2.9 提现至工作台余额（13，已打通）
- 已新增提现接口：`POST /shopee/runs/{run_id}/finance/withdraw`。
- 已落地提现核心规则：
  - 必须存在默认银行卡；
  - 提现金额需大于 0 且不超过钱包余额；
  - 固定汇率：`1 RM = 1.74 RMB`。
- 已实现双侧落库（同事务）：
  - Shopee 侧写入 `shopee_finance_ledger_entries`（`entry_type=withdrawal`、`direction=out`）；
  - 工作台侧写入 `game_run_cash_adjustments`（`source=shopee_withdrawal`、`direction=in`）。
- 已打通工作台资金口径：
  - `procurement/cart-summary` 改为 `initial_cash + 调账净额 - 各环节支出`；
  - 提现后“总资金/采购剩余”可在工作台实时体现。
- 已按业务口径优化工作台“阶段总览数据”展示：
  - `期初资金`
  - `累计收入（仅提现转入）`
  - `累计支出（采购/物流/仓储）`
  - `当前可用余额`
  - 明确不在工作台侧展示 Shopee 回款收入（仅在 Shopee 财务页展示）。
- 已新增工作台资金明细 Tab（右侧卡片）：
  - `收入明细`：仅展示提现转入记录；
  - `支出明细`：展示采购/物流/仓储逐笔记录；
  - 每条记录按 `游戏日 + 游戏时刻` 展示时间。
- 已新增后端明细接口：`GET /game/runs/{run_id}/finance/details?tab=income|expense&page=&page_size=`。
- 已完成前端交互：
  - `我的余额` 点击“提现”可弹窗输入金额；
  - 展示“预计转入工作台（RMB）”与固定汇率提示；
  - 提现成功后自动刷新余额总览与流水。
- 已补关键测试：
  - 提现成功后钱包扣减、工作台总资金增加；
  - 无默认银行卡时提现被拦截。

### 2.10 选品采购-海外仓-Shopee库存补货闭环（14，进行中）
- 已完成数据层第一批打通（模型+历史库补字段+注释）：
  - `inventory_lots`：`reserved_qty/backorder_qty/last_restocked_at`
  - `shopee_listing_variants`：`oversell_limit/oversell_used`
  - `shopee_orders`：`listing_id/variant_id/stock_fulfillment_status/backorder_qty/must_restock_before_at`
- 已完成订单模拟双通道改造：
  - 支持“现货产单 + 超卖产单”
  - 超卖额度不足时拒单并记录 `oversell_limit_reached`
  - 订单落库写入缺货履约语义字段（待补货状态与补货截止时间）
- 已完成发货阻断口径：
  - 缺货单（`backorder_qty > 0`）不可直接发货，返回业务错误“待补货，暂不可发货”。
- 已完成取消回补精确化：
  - 优先按 `order.listing_id/variant_id` 定位回补目标
  - 回滚销量，并释放超卖占用（`oversell_used`）
- 已完成前端首版展示打通：
  - 我的订单：待出货展示“现货可发 / 待补货（缺口）/ 最晚补货时间”

### 2.11 对局7天结束与Shopee订单封盘设计（16，已完成设计）
- 已新增设计文档：`docs/设计文档/16-7天对局结束与Shopee订单封盘设计.md`。
- 设计已明确采用“入口闸门方案”：
  - 超过 `duration_days` 后，Shopee 订单域停止自动模拟、自动取消、自动物流推进、自动回款补记。
  - 后台 auto tick worker 与手动/admin simulate 在封盘后统一跳过或拒绝。
- 设计已明确“结束后可新开局”机制：
  - run 到期后状态更新为 `finished`（幂等），解除“已有 running 对局”拦截。
  - 新 run 以新 `run_id` 独立启动，经营数据从 0 开始。
- 已按需求补充“历史经营记录”口径：
  - 历史入口固定为工作台左侧菜单 `/setup/history`。
  - 历史列表仅展示 `finished` 对局。
  - 支持从历史页携带 `run_id` 跳转各阶段全链路只读回溯。
  - 历史回溯下经营动作按钮保留，但点击统一提示只读，不执行写入。

## 3. 当前阶段
- 当前阶段：`Step 05 店铺运营（Shopee）` 的“对局时长封盘约束”设计已完成，进入后端实现与回归验证阶段。

## 4. 下一步待办
- 按设计实现统一结束判定函数，并在 Shopee 自动演化入口与 worker 入口接入闸门。
- 接入 run 到期自动置 `finished` 的幂等更新逻辑。
- 补充边界回归：第7天内可演化、第8天后封盘、封盘后可新建 run。
- 若涉及订单缓存/simulate 缓存行为改动，执行 Redis 订单缓存回归脚本并记录结果。
  - 我的产品：变体展示“超卖已用/超卖上限”
- 已完成 Shopee 上架“仓库关联商品”首版打通：
  - 新增后端接口 `GET /shopee/runs/{run_id}/warehouse-link-products`，按 run 聚合返回可关联入仓商品与库存概览；
  - 商品发布接口 `POST /shopee/runs/{run_id}/product-drafts/{draft_id}/publish` 新增 `source_product_id` 参数并落库 `shopee_listings.product_id`；
  - 新增 live 发布强校验：未关联仓库商品时阻断发布；
  - 前端新增商品页右侧卡片由“Shopee 标准商品”替换为“仓库关联商品”，支持搜索、分页、单选关联与编辑回显。
- 已完成库存口径打通修复（后端）：
  - 新增 `inventory_lot_sync` 统一分配器，打通 `available <-> reserved` 与发货出库扣减。
  - 下单占用：订单模拟时同步将 `inventory_lots.quantity_available` 转入 `reserved_qty`。
  - 取消释放：取消订单时同步将 `reserved_qty` 释放回 `quantity_available`（含历史数据回补兜底）。
  - 发货出库：安排发货时同步扣减 `reserved_qty`，并写入 `order_ship` 库存流水。
  - 补货冲减：Step04 入库自动冲减待补货时，同步把可用库存转为预占库存。
- 已完成 Step04 入库补货联动：
  - 入库确认后，按商品维度自动扫描并冲减最早创建的 `backorder` 订单缺口；
  - 缺口冲减为 0 的订单自动转为 `restocked`，清空最晚补货时间，可恢复发货。
- 已新增库存流水台账：`inventory_stock_movements`
  - 已落库 `purchase_in / order_reserve / cancel_release / restock_fill` 四类流水；
  - 支持按 run/user/商品/变体/订单追溯库存变化来源与方向。
- 已新增库存流水查询接口：`GET /game/runs/{run_id}/warehouse/stock-movements`
  - 支持分页、按变动类型筛选、按业务单号/备注关键字检索；
  - 返回结构包含三类库存增减（现货/预占/待补货）与业务关联字段（biz_ref/remark）。
- 已完成 Step04 海外仓页面内聚展示：
  - 同页双 Tab：`入仓决策` / `库存变动明细`；
  - 右侧新增“最近库存变动（5条）”预览卡，并支持一键跳转明细 Tab；
  - 明细 Tab 支持分页浏览与筛选查询，减少跨页面跳转成本。
- 已新增缺货风险看板接口与页面展示：
  - 后端新增 `GET /game/runs/{run_id}/warehouse/backorder-risk?top_n=5`；
  - 汇总口径包含：受影响订单数、缺货总量、24小时内风险单、已超时风险单、预估取消损失；
  - Step04 右侧新增“缺货风险看板”卡片，展示 Top SKU/商品缺口与最早截止时间。
- 已新增 Step04 库存总览接口与页面展示：
  - 后端新增 `GET /game/runs/{run_id}/warehouse/stock-overview`；
  - 统一返回口径：库存SKU、总库存（可用+预占+锁定）、可用库存、已占用、待补货；
  - 前端在 `库存变动明细` Tab 顶部新增 5 张总览卡，支持快速查看当前海外仓库存状态。
- 已修复库存占用与发货口径一致性问题（后端）：
  - 订单模拟阶段改为按 `inventory_lots` 实际预占数量确定现货扣减，不再按“计划扣减”直接记流水；
  - 当库存批次与商品展示库存不一致导致预占不足时，自动转入 `backorder_qty`，并继续受超卖上限约束；
  - 发货接口新增预占校验：预占不足或订单缺少商品映射时阻断发货，避免出现“已发货但海外仓库存未变化”的异常。
- 已新增任务清单：`docs/task/14-选品采购-海外仓-Shopee库存订单补货闭环-任务清单.md`。

### 2.11 Shopee 订单模拟日志查询性能修复（进行中）
- 已完成日志“最新 tick”读取口径统一：将多处 `ORDER BY tick_time DESC, id DESC LIMIT 1` 改为 `MAX(tick_time)` 聚合查询，覆盖玩家端、管理员端与自动 tick worker，减少大表排序开销并规避 `Out of sort memory`。
- 已在 `shopee_order_generation_logs` 增加索引兜底（模型声明 + 启动初始化补齐）：
  - `ix_shopee_order_generation_logs_run_user_tick_id (run_id, user_id, tick_time, id)`
  - `ix_shopee_order_generation_logs_run_user_created_at (run_id, user_id, created_at)`
- 下一步：线上环境重启后确认索引创建成功，并对关键接口执行 `EXPLAIN` 复核执行计划。

### 2.12 Redis 订单列表缓存试点（进行中）
- 已完成首个接口试点：`GET /shopee/runs/{run_id}/orders` 增加短 TTL 缓存读取与回填（按 run/user/查询参数哈希分 key）。
- 已完成写后立即可见（Read-After-Write）首批失效接入：
  - `POST /shopee/runs/{run_id}/orders/simulate`
  - `POST /game/admin/runs/{run_id}/orders/simulate`
  - `POST /shopee/runs/{run_id}/orders/{order_id}/ship`
  - `POST /shopee/runs/{run_id}/orders/{order_id}/cancel`
  - `POST /shopee/runs/{run_id}/orders/{order_id}/logistics/progress`
- 已新增 Redis 缓存基础封装：`app/core/cache.py`（可开关、超时、异常降级、前缀失效）。
- 已新增本地一键验收脚本：`backend/apps/api-gateway/scripts/verify_redis_orders_cache.py`（登录、触发读缓存、校验 key/ttl、simulate 后失效与重建全流程）。
- 已将 Redis 回归脚本命令纳入 `AGENTS.md` 默认检查项（涉及订单缓存/失效逻辑改动时提交前必跑）。
- 已完成 Redis P1 基础能力落地：
  - 新增分布式锁模块 `app/core/distributed_lock.py`（simulate 与 auto tick 互斥）。
  - 新增限流模块 `app/core/rate_limit.py`（按用户窗口计数，支持开关降级）。
  - 玩家/管理员订单模拟接口已接入“限流 + 分布式锁”。
  - 订单列表接口已接入读接口限流保护。
- 已完成 Redis P2 看板缓存扩展（短 TTL）：
  - `GET /game/admin/buyer-pool/overview`
  - `GET /game/runs/{run_id}/warehouse/summary`
  - `GET /game/runs/{run_id}/warehouse/stock-overview`
  - `GET /game/runs/{run_id}/warehouse/backorder-risk`
- 已补齐配置项模板（`.env.example` / `.env.deploy` / `docker-compose.aliyun-2027.yml`）：
  - `REDIS_LOCK_TTL_SEC`
  - `REDIS_RATE_LIMIT_ENABLE`
  - `REDIS_RATE_LIMIT_SIMULATE_PER_MIN`
  - `REDIS_RATE_LIMIT_ADMIN_SIMULATE_PER_MIN`
  - `REDIS_RATE_LIMIT_ORDERS_LIST_PER_MIN`
  - `REDIS_CACHE_TTL_OVERVIEW_SEC`

### 2.13 7天对局结束与 Shopee 订单封盘（16，已完成后端首版）
- 已完成 run 到期自动收口能力：
  - 新增统一到期判定逻辑，命中后将 `running -> finished`（幂等）。
  - `POST /game/runs` 在校验重复 running 前，先自动收口当前用户已到期 run，避免阻塞新开局。
- 已完成 Shopee 订单域封盘闸门：
  - 查询类接口（订单/物流/财务）支持 `finished` run 继续只读查询。
  - 演化写接口（simulate/发货/取消/物流推进）在 `finished` run 下统一返回 `400`。
- 已补齐工作台历史回看能力（Step02~Step04）：
  - `GET /game/runs/current` 现在可返回 `finished` run（优先 running，其次 finished）。
  - `game` 域查询接口（采购/物流/仓储/经营资金明细）支持 `finished` run 只读访问。
  - `game` 域写接口（采购下单/创建物流/仓储策略与入仓等）在 `finished` run 下统一拒绝写入。
- 已完成 auto tick worker 封盘适配：
  - worker 每轮处理前执行结束判定，到期 run 直接标记 `finished` 并跳过模拟。
- 已完成参数约束更新：
  - `POST /game/runs` 的 `duration_days` 下限由 `30` 调整为 `7`，支持 7 天对局玩法。
- 已补关键自动化测试（通过）：
  - 7天开局成功、到期 run 自动收口、新局可创建；
  - finished run 查询可用且不触发自动写入；
  - finished run 的 simulate/admin simulate 被拒绝；
  - auto tick worker 对到期 run 跳过并置 finished。
- 已完成“历史经营记录”回溯能力首版实现（前后端联动）：
  - 新增接口 `GET /game/runs/history/options`（仅返回当前玩家 `finished` 对局列表）。
  - 新增接口 `GET /game/runs/{run_id}/history/summary`（历史对局总结聚合）。
  - 新增接口 `GET /game/runs/{run_id}/context`（URL `run_id` 回溯上下文）。
  - 工作台 `/setup/history` 已接入历史列表 + 单局总结，并支持携带 `run_id` 跳转 Step02~Step05。
  - App 已支持 `run_id` 路由解析与阶段跳转透传；历史局进入各阶段时自动切换为只读回溯模式。
  - Step02/03/04 与 Shopee 关键写操作按钮保留，点击会提示“历史只读”，不再执行经营写入。
  - 已补 Step03/Step04 历史回溯细节修复：只读模式下发运/清关/仓型仓位/入仓等经营交互统一禁用（不可点击），Step04 追加历史总结兜底读取，避免历史局入仓批次与库存汇总显示缺失。
- 已补 Step04 历史决策面板回显修复：历史回溯下 C 区块改为展示“历史入仓批次/件数/货值 + 历史仓储费用快照”，并将 `warehouse/summary` 扩展为返回 `completed_inbound_total_quantity` 与 `completed_inbound_total_value`，同时 finished 局无 active 策略时回退最近一次策略。
- 已统一 Step01 开局资金为固定 `300,000 RMB`：
  - 前端开局页改为只展示“300,000 RMB（统一）”，移除 100,000/200,000 可选入口；
  - 后端 `POST /game/runs` 保持按请求值落库；当前统一口径由前端固定传入 `300000` 实现。

## 3. 后端与数据层进展
- FastAPI + SQLAlchemy 主体接口持续扩展，经营关键状态持续落库。
- 管理员相关接口已提供：
  - `GET /game/admin/runs/options`
  - `GET /game/admin/buyer-pool/overview`
- 已建立买家画像表（含表注释与字段注释）并完成初始化数据写入。

## 4. 文档进展
- 设计文档已覆盖至 `09`（履约发货与物流时效结算）。
- 已沉淀“仓点-买家距离驱动 ETA、物流轨迹、安排发货、结算净入账”方案。
- `09` 设计文档已补充“Step03 线路选择 -> Step05 履约时效”接通方案：按 Step03 线路上限（如快速线最多 8 天）+ 买家距离分档，计算订单运输游戏天数并约束签收触发。
- 已新增 `10-买家取消订单（超时未发货）设计`（仅取消，退货退款后续阶段实现）。
- 已新增 `11-卖家回款与余额流水设计`（定义回款入账、流水落库、余额/收入页面接口与验收口径）。
- 已新增 `12-添加银行账户设计`（定义银行账户新增、默认账户管理、余额页联动）。
- 已新增 `13-提现至工作台余额设计`（定义提现输入、固定汇率 `1 RM = 1.74 RMB`、Shopee 钱包扣减与工作台资金联动口径）。
- 已新增 `14-选品采购-海外仓-Shopee库存订单补货闭环设计`（定义“海外仓真实库存口径 + Shopee可控超卖上限 + 缺货待补 + 补货回流 + 取消/回款联动”闭环方案）。
- 已新增 `15-Shopee上架商品仓库关联设计`（定义“新增商品页仓库关联卡片 + 发布绑定 product_id + 库存口径打通校验”方案）。
- 已更新 `16-7天对局结束与Shopee订单封盘设计` 为可实施版本（补齐 finished 只读可查、写接口封盘、create_run 前自动收口到期 run、`duration_days>=7` 约束建议与统一验收矩阵）。
- 已新增 `17-Shopee商品多模态内容质量评分设计`（定义规则引擎+视觉模型+文本模型融合评分、`quality_status` 阈值、`.env` 全局模型配置、评分数据模型与异步重评接口方案）。
- 已完成 17 文档首版代码落地（前后端+数据库）：
  - 数据层：新增 `shopee_listing_quality_scores` 评分快照表；`shopee_listings` 新增 `quality_total_score / quality_scored_at / quality_score_version`；
  - 后端：新增质量评分服务（规则引擎 + 可选模型调用降级）、列表返回分数、评分详情接口、重评接口，并在商品发布/创建后自动评分；
  - 前端：`我的产品` 内容质量列改为“状态+分数”，`提升` 按钮接入重评，`更多` 可查看分项与建议。
- 已完成质量评分模型调用优化：由“文本/一致性/视觉多次调用”调整为“单次多模态调用返回多维分项”，减少重复请求与评分波动。
- 已增强质量评分 Ollama 兼容性与排障可观测性：
  - `provider=ollama` 时不再强制 `response_format=json_object`，避免部分 Ollama 版本/模型返回 400；
  - 增加模型调用失败日志（HTTP 状态/错误详情/空响应/非 JSON 内容），定位“模型不可用已降级”原因更直接。
- 已进一步完善 Ollama 本地调用链：
  - `provider=ollama` 优先使用原生 `/api/chat`（`format=json`，支持 base64 图片）并保留 OpenAI 兼容接口兜底；
  - 评分图片输入扩展为“商品图 + SKU 变体图”合并去重后送模；
  - 评分降级文案改为 Ollama 专项提示，便于快速定位服务、模型能力与图片可访问性问题。
- 已修复评分口径偏差：规则引擎与视觉启发式中的“商品图数量”已改为按“主图 + SKU 变体图（去重）”统计，不再仅按主图表计数。
- 已增强质量详情可读性：
  - 质量详情接口新增逐图评价输出（从模型原始返回提取 `image_feedback`）；
  - 前端质量详情弹窗取消“仅前3条”截断，改为完整展示主要问题/优化建议，并新增“逐图评价（每张图好/坏/建议）”区块。
- 已完成质量详情弹窗体验升级（frontend）：
  - “点击内容质量”由系统 `alert` 升级为页面内结构化弹窗（分数卡片、维度区块、问题建议区、逐图评价区）；
  - 逐图评价展示从 URL 改为业务标签（主图 / SKU 编号或变体名），避免暴露公开图片 URL；
  - 评分维度在弹窗内显式展示（规则/图像/文本/一致性子维度），便于解释模型评分依据。
- 已按 Shopee 视觉风格完成二次调整：
  - 质量详情弹窗改为橙色主视觉与轻量边框风格，贴近 Shopee 站内调性；
  - 内容结构调整为“综合评分 / 评价总结 / 维度详细分析与评分 / 专业建议与优化方向 / 逐图评价”；
  - 后端逐图评价提取新增 `image_inputs` 补齐兜底，确保主图/SKU图即便模型漏回也不会在详情中消失。
- 已新增新用户说明文档：`docs/说明文档/01-新用户最小可用操作说明.md`（含操作路径、资金口径、提现流程、常见问题）。
- 已新增阿里云部署说明文档：`docs/说明文档/02-阿里云轻量服务器Docker部署说明.md`（Docker Compose 部署到 `112.124.32.196:2027`，复用现有 MySQL 容器配置）。
- 已增强新用户说明文档中 `Step05 店铺运营（Shopee）` 章节：细化到页面级能力、操作顺序与注意事项，便于新用户按图索骥上手。
- 已补充新用户说明文档中的 Shopee 右侧提示/通知区说明（入口、提示内容、使用建议）。
- 已新增任务清单 `11-卖家回款与余额流水-任务清单`（P0~P5）。
- 已实现 Shopee 通知抽屉同层挤压布局（MVP）：
  - 右侧通知区由 `NotificationDrawer` 承载，打开后主内容同层左压缩，不使用覆盖蒙层；
  - 抽屉开关由 `ShopeePage` 全局状态统一管理，跨 Shopee 子页面保持打开状态；
  - 触发入口接入右侧工具栏通知按钮。
- 已将通知抽屉从前端写死数据切换为真实订单数据：
  - 红点数量改为“待处理发货订单数”（`toship + source=to_process`），并按 `99+` 封顶显示；
  - 抽屉列表改为真实待处理订单（订单号/买家/金额/倒计时/下单时间），移除 mock 通知填充内容；
  - 通知数据由页面层定时刷新（30s）并统一透传到右侧栏与通知抽屉。
- 已新增 Redis 设计目录与首版规划文档：`docs/redis设计/01-Redis接入与并发性能优化设计.md`（覆盖目标、流程、规则、Key 设计、接口改造清单、验收标准与分阶段实施）。
- 已完成 Shopee 单品折扣创建页首版接入（基于 20 号设计文档）：
  - 前端新增 `DiscountCreateView`，已接上 `营销中心 -> 折扣 -> 单品折扣 -> 创建` 路由；
  - 页面复刻完成基础信息区、活动时间、单品折扣商品区、添加商品弹窗、取消/确认页脚；
  - 折扣首页“单品折扣”的创建按钮已从占位提示改为真实跳转。
- 已完成单品折扣创建页后端首版接口：
  - `GET /shopee/runs/{run_id}/marketing/discount/create/bootstrap`
  - `GET /shopee/runs/{run_id}/marketing/discount/eligible-products`
  - `POST /shopee/runs/{run_id}/marketing/discount/drafts`
  - `GET /shopee/runs/{run_id}/marketing/discount/drafts/{draft_id}`
  - `POST /shopee/runs/{run_id}/marketing/discount/campaigns`
- 已完成单品折扣创建页数据层首版：
  - 新增 `shopee_discount_drafts`
  - 新增 `shopee_discount_draft_items`
  - 已补齐表注释与字段注释，并接入创建页 / 可选商品 / 草稿缓存与限流设计。
- 已完成单品折扣创建页前端二次对齐：
  - 创建页日期时间选择器已抽为 Shopee 公共组件 `DateTimePicker`，创建页与原商品页共用同一套交互逻辑；
  - 创建页基础信息区表单排版已调整，修复日期区与辅助文案重叠问题；
  - 进入单品折扣创建页时已隐藏左侧菜单栏，贴近 Shopee 官方创建页布局。
- 已完成单品折扣“添加商品”弹窗首轮复刻：
  - 弹窗已对齐 Shopee 官方结构，补齐标题、双 Tab（选择商品 / 上传商品列表）、筛选条、结果表头、空态与底部操作区；
  - 继续复用现有可选商品接口，当前“上传商品列表”先保留结构占位，后续再接入真实导入流程；
  - 选择 Tab 已支持分类下拉、搜索字段下拉、关键词输入、搜索/重置、仅显示可参与商品勾选与批量全选。
- 已将“添加商品”弹窗中的店铺分类菜单接为真实数据：
  - 可选商品接口已补充分类路径字段，前端不再使用写死演示子类；
  - “店铺分类”二级菜单改为按当前店铺可选商品中的真实分类去重生成；
  - 选择某个店铺分类后，会按该分类过滤弹窗中的可选商品列表。
- 已将单品折扣创建页的“活动时长必须小于 180 天”改为真实可用限制：
  - 日期时间选择器已支持最小/最大可选时间边界；
  - 创建页结束时间现在会受“开始时间 + 最多 180 天内”约束，开始时间也会受结束时间反向约束；
  - 超出范围时页面会即时显示红色错误提示，并继续禁止提交。
- 已调整单品折扣创建页默认活动时间：
  - 新开创建页时，默认开始时间改为浏览器当前时刻，结束时间默认顺延 1 小时；
  - 若存在草稿，则仍优先使用草稿中的开始/结束时间回填，不覆盖用户已保存内容。
- 已新增套餐优惠创建页设计文档：`docs/设计文档/21-Shopee套餐优惠创建页设计.md`
  - 已按 Shopee 官方 `Create New Bundle Deal` 页面拆分基础信息、套餐类型、阶梯规则、移动端预览、商品区、页脚操作等核心区块；
  - 已补齐前端路由与组件拆分建议、后端 bootstrap/草稿/创建/详情接口、数据库主表/商品表/阶梯表/草稿表，以及 Redis key、TTL 与失效策略；
  - 已明确与 19 号折扣页、20 号单品折扣创建页的衔接关系，后续实现可直接沿用现有创建页容器、公共日期组件与商品选择器基础设施。
- 已完成 Shopee 套餐优惠创建页首版实现：
  - 前端新增 `BundleCreateView`，已接入 `/shopee/marketing/discount/create?type=bundle` 分流，复刻基础信息、套餐类型、阶梯编辑、移动端预览、商品选择弹窗与底部操作区；
  - 后端新增 `marketing/bundle/create/bootstrap`、`marketing/bundle/eligible-products`、`marketing/bundle/campaigns` 接口，并复用现有营销活动主表将套餐类型、限购次数、阶梯规则写入 `rules_json`；
  - 折扣列表中的“套餐优惠”创建卡片已可直达新页面，创建成功后会进入现有折扣活动列表并按 `bundle` 类型展示。

## 5. 当前待办（短期）
- 履约链路下一步：将物流节点从“查询时补推进”升级为“独立 tick 调度任务”。
- 取消模块补测试：48h 阈值边界、自动取消命中/未命中、非 toship 手动取消拒绝。
- 完成自动任务接入（游戏 tick 每小时自动调用 simulate，避免重复触发）。
- 增强订单生成日志可视化（按时段趋势、失败原因占比）。
- 优化模拟参数面板（阈值/时段系数可配置化，而非固定代码）。
- 清理前端历史 TypeScript 报错（非本模块新增问题）。
- 补齐提现链路自动化测试（余额不足、超小数精度、并发重复提交场景）。

## 6. 风险与说明
- 目前系统仍处于高频迭代阶段，部分页面细节持续对齐中。
- 复杂竞争分流（多玩家同品竞争）暂未实装，先按单玩家 MVP 路径推进。
